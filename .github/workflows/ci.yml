name: CI

# Runs on pull requests (so PR contributors get fast type/clippy feedback)
# and on manual dispatch (so you can re-run after force-pushing or to
# spot-check the main branch). We deliberately do NOT trigger on every
# push to main — per maintainer preference, only manual builds.
on:
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check-version-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: ./.github/scripts/check-version-sync.sh

  check-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsc --noEmit

  check-backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf
      - run: cd src-tauri && cargo check
      - run: cd src-tauri && cargo test
      - run: cd src-tauri && cargo clippy -- -D warnings
